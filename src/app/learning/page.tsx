import LearningPage from "@/pages/LearningPage/LearningPage";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create your event | PartyApp",
};

export default function Learning() {
  return <LearningPage />;
}
